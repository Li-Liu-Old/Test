Success！！！



删除本地分支，切换分支，pull

git pull origin master（：local 可省略）  取origin的master分支，与本地的local分支（如果与当前分支，可忽略）合并

git fetch origin master（从远程的origin仓库的master分支获取最新版本到本地）

git clone xxx，git branch -a，git remote add 新建本地分支名 xxx，修改文件，git commit -m "v01"，git push origin 本地分支名 远程分支名（git push 本地分支名）。